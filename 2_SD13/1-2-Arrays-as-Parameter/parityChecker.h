#pragma once

/*
	Function:	checks if indexes are equel in 2 arrays
	Parameters:	array1:	first array to be checked
				array2: second array to be checked
				array3: result array
*/

void checkPairs(const int* array1, const int* array2, int* array3, int maxSize);