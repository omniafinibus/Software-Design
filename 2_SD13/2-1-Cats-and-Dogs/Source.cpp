#include <iostream>
#include <string>

using namespace std;

int main()
{
	string input;
	string cat = cat;

	cout << "Please enter a line of text:\n";
	getline(cin, input);

	if (input.find(cat) == true)
	{
		input.replace();
	}

	cout << "\n";
	cout << input << "\n";

	system("PAUSE");
}